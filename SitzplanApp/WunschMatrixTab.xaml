<UserControl x:Class="SitzplanApp.Views.WunschMatrixTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SitzplanApp">

    <UserControl.Resources>
        <local:HexToBrushConverter x:Key="HexBrush"/>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Kopfzeile: Legende + Aktionen -->
        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <Button DockPanel.Dock="Right" Content="Alle Wünsche löschen"
                    Click="BtnAllesLoeschen_Click"
                    Background="#C0392B" Foreground="White" BorderThickness="0"
                    Padding="10,5" FontSize="11" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="14" Height="14" CornerRadius="3" Background="#27AE60"/>
                <TextBlock Text="Zusammen" FontSize="11" Foreground="#444" Margin="4,0,12,0" VerticalAlignment="Center"/>
                <Border Width="14" Height="14" CornerRadius="3" Background="#C0392B"/>
                <TextBlock Text="Nicht neben" FontSize="11" Foreground="#444" Margin="4,0,16,0" VerticalAlignment="Center"/>
                <TextBlock FontSize="11" Foreground="#888" VerticalAlignment="Center"
                           Text="Linksklick zykliert · Rechtsklick löscht · immer beidseitig"/>
            </StackPanel>
        </DockPanel>

        <!-- Meldung (z. B. max. 2 erreicht) -->
        <Border Grid.Row="1" Background="#FDECEA" CornerRadius="4" Padding="8,5" Margin="0,0,0,8">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Meldung}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding Meldung}" FontSize="11" Foreground="#922B21"/>
        </Border>

        <!-- Hinweis wenn nichts geladen -->
        <TextBlock Grid.Row="2" Text="Excel laden, um die Matrix zu füllen."
                   FontSize="12" Foreground="#999" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HatDaten}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Matrix mit fixierten Headern -->
        <Grid Grid.Row="2">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HatDaten}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Ecke -->
            <Border Grid.Row="0" Grid.Column="0" Width="150" Height="30"
                    Background="#F3F4F6" BorderBrush="#E5E7EB" BorderThickness="0,0,1,1"/>

            <!-- Kopfzeile (Spaltennummern) -->
            <ScrollViewer x:Name="svTop" Grid.Row="0" Grid.Column="1"
                          VerticalScrollBarVisibility="Hidden"
                          HorizontalScrollBarVisibility="Hidden">
                <ItemsControl ItemsSource="{Binding SpaltenKoepfe}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="30" Height="30" Background="#F3F4F6"
                                    BorderBrush="#E5E7EB" BorderThickness="0,0,1,1">
                                <TextBlock Text="{Binding}" FontSize="10" Foreground="#555"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Kopfspalte (Nr + Name) -->
            <ScrollViewer x:Name="svLeft" Grid.Row="1" Grid.Column="0"
                          VerticalScrollBarVisibility="Hidden"
                          HorizontalScrollBarVisibility="Hidden">
                <ItemsControl ItemsSource="{Binding Zeilen}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="150" Height="30" Background="#FAFAFA"
                                    BorderBrush="#E5E7EB" BorderThickness="0,0,1,1">
                                <TextBlock Text="{Binding Kopf}" FontSize="11" Foreground="#333"
                                           Margin="6,0,4,0" VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Zellen -->
            <ScrollViewer x:Name="svMain" Grid.Row="1" Grid.Column="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto"
                          ScrollChanged="svMain_ScrollChanged">
                <ItemsControl ItemsSource="{Binding Zeilen}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding Zellen}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="30" Height="30" Cursor="Hand"
                                                Background="{Binding Farbe, Converter={StaticResource HexBrush}}"
                                                BorderBrush="#E5E7EB" BorderThickness="0,0,1,1"
                                                ToolTip="{Binding Tooltip}"
                                                MouseLeftButtonUp="Zelle_Left"
                                                MouseRightButtonUp="Zelle_Right">
                                            <TextBlock Text="{Binding Glyph}" Foreground="White"
                                                       FontFamily="Segoe UI Semibold" FontSize="14"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
