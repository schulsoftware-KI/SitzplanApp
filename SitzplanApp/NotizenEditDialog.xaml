<Window x:Class="SitzplanApp.Views.NotizenEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Titel}"
        Width="440" Height="240"
        MinWidth="320" MinHeight="200"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="#F7F9FC"
        ShowInTaskbar="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titel -->
        <TextBlock Grid.Row="0"
                   Text="{Binding Titel}"
                   FontSize="14" FontWeight="Bold"
                   Foreground="#1F3864"/>

        <!-- Notiz-Textfeld -->
        <Border Grid.Row="2"
                BorderBrush="#2E75B6" BorderThickness="1.5"
                CornerRadius="3" Background="White">
            <TextBox x:Name="txtNotiz"
                     Text="{Binding Notiz, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     FontSize="13"
                     Padding="8,6"
                     BorderThickness="0"
                     Background="Transparent"
                     VerticalAlignment="Stretch"/>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Abbrechen"
                    Click="BtnAbbrechen_Click"
                    Padding="18,6" FontSize="13"
                    Background="#E8E8E8" Foreground="#333"
                    BorderThickness="0" Cursor="Hand"
                    Margin="0,0,10,0"/>
            <Button Content="💾  Speichern"
                    Click="BtnSpeichern_Click"
                    IsDefault="True"
                    Padding="18,6" FontSize="13"
                    Background="#1F3864" Foreground="White"
                    BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
